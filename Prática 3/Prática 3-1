#include <stdio.h>
#include<stdlib.h>
#include<math.h>

int main(void) {
  int num;
  scanf("%d",&num);
  if(num%2==0)
  {
  printf("Esse número é par");
  }
  else
    printf("Esse número é ímpar");
  }
